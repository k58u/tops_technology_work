#   What happens when „1‟== 1 is executed?

"""
When "1" == 1 is executed in Python, 
it returns False because "1" is a string, and 1 is an integer. 
They are different types, so they are not equal.

result = "1" == 1
print(result)  

# output will be ( it will print False )

"""